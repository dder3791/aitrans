<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.web.dao.AgreetmentDao">
	 
	 <!-- 查询所有协议信息 -->
	 <select id="queryByAll" parameterType="string" resultType="Agreetment">
	  select * from agreetmentD where agreetment=#{agreetment}
	 </select>
 
</mapper>
